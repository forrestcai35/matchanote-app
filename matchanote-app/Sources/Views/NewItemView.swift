import SwiftUI

struct NewWrittenNoteView: View {
  var body: some View {
    Text("New Item")
  }
}

struct NewFolderView: View {
  var body: some View {
    Text("New Folder")
  }
}

struct ImportView: View {
  var body: some View {
    Text("Import")
  }
}
